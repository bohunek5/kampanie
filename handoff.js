import { prescotLogo } from './brand.js?v=4.0.1';
import { icon } from './icons.js?v=4.0.1';
import { goalMeta } from './model.js?v=4.0.1';
import { products, nodeTypes } from './workspace.js?v=4.0.1';
import { cleanHandoff, cleanDesign, handoffFields, designFields, formatExamples, selectedCreatives, handoffGaps } from './production-model.js?v=4.0.1';
const esc=value=>String(value??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const missing='<span class="hb-missing">Do uzupełnienia</span>';
function linkedText(value){
 if(!value?.trim())return missing;
 return value.split(/(https?:\/\/[^\s<>"']+)/g).map(part=>{
  if(/^https?:\/\//.test(part)){try{const url=new URL(part);if(['https:','http:'].includes(url.protocol))return `<a href="${esc(url.href)}" target="_blank" rel="noopener noreferrer">${esc(part)}</a>`;}catch{}}
  return esc(part);
 }).join('');
}
export function creativeContext(campaign,id){
 const seen=new Set([id]),queue=[id],nodes=[];
 while(queue.length){const current=queue.shift();for(const e of campaign.flow.edges.filter(e=>e.to===current)){if(seen.has(e.from))continue;seen.add(e.from);const node=campaign.flow.nodes.find(n=>n.id===e.from);if(node){nodes.push(node);queue.push(node.id);}}}
 return {channels:nodes.filter(n=>n.type==='channel'),brief:nodes.filter(n=>['product','audience','message'].includes(n.type)).reverse()};
}
const entry=(label,value)=>`<div class="hb-entry"><dt>${label}</dt><dd>${linkedText(value)}</dd></div>`;
function body(campaign){
 const common=cleanHandoff(campaign.handoff),nodes=selectedCreatives(campaign),gaps=handoffGaps(campaign),name=campaign.brief.name||'Kampania bez nazwy';
 return `<article class="handoff-paper"><header class="hb-header"><div class="hb-brand">${prescotLogo}<span>BRIEF DLA GRAFIKA</span></div><span class="hb-date">Zapis z ${new Date().toLocaleString('pl-PL')}</span></header><div class="hb-title"><span>ZLECENIE KREACJI</span><h1>${esc(name)}</h1><p>${esc(products[campaign.product].name)} · ${esc(goalMeta[campaign.goal].label)} · Liczba wybranych materiałów: ${nodes.length}</p></div>${gaps.length?`<aside class="hb-attention"><strong>Do uzgodnienia przed wykonaniem</strong><ul>${gaps.map(g=>`<li>${esc(g)}</li>`).join('')}</ul></aside>`:''}<section class="hb-section"><h2>01 / Kontekst kampanii</h2><dl class="hb-grid">${entry('Co promujemy?',campaign.brief.offer)}${entry('Dla kogo?',campaign.brief.audience)}${entry('Potwierdzony wyróżnik oferty',campaign.brief.advantage)}</dl></section><section class="hb-section"><h2>02 / Organizacja pracy</h2><dl class="hb-grid">${entry('Grafik / studio',common.designer)}${entry('Osoba zbierająca uwagi',common.reviewer)}${entry('Termin pierwszej wersji',common.due)}</dl><dl>${entry('Folder z materiałami',common.assets)}${entry('Wspólne wytyczne',common.requirements)}${entry('Sposób oddania plików',common.delivery)}</dl></section><section class="hb-section"><h2>03 / Materiały do przygotowania</h2>${nodes.length?nodes.map((node,i)=>{
  const d=cleanDesign(node.design),context=creativeContext(campaign,node.id);
  return `<section class="hb-material"><div class="hb-material-heading"><span>${String(i+1).padStart(2,'0')}</span><div><h3>${esc(node.title)}</h3><p>${esc(context.channels.map(n=>n.title).join(' / ')||'Kanał do ustalenia')}</p></div></div><dl class="hb-grid">${entry('Format i rozmiar',d.format)}${entry('Osoba',node.owner||common.designer)}${entry('Termin',node.due||common.due)}</dl><dl>${entry('Pomysł zapisany na mapie',node.note)}${entry('Nagłówek na kreacji — dokładny tekst',d.headline)}${entry('Tekst towarzyszący reklamie',d.body)}${entry('Wezwanie do działania (CTA)',d.cta)}${entry('Kierunek wizualny / scenariusz',d.visual)}${entry('Materiały do tej kreacji',d.assets||common.assets)}${entry('Docelowa strona reklamy',d.destination)}</dl>${context.brief.length?`<div class="hb-context"><h4>Ustalenia poprzednich etapów tej gałęzi</h4><dl>${context.brief.map(n=>entry(`${nodeTypes[n.type].label} · ${esc(n.title)}`,n.note)).join('')}</dl></div>`:''}</section>`;
 }).join(''):'<p>Nie wybrano materiałów do przekazania.</p>'}</section><section class="hb-section hb-delivery"><h2>04 / Przekazanie projektu i poprawek</h2><ol><li>Oznacz pliki nazwą kreacji i wersją, np. <b>profile-detal-A-v01</b>.</li><li>Pierwszy podgląd przekaż osobie zbierającej uwagi. Wskaż brakujące materiały lub ustalenia.</li><li>Poprawki zapisuj do konkretnej wersji: nazwa pliku, zakres zmiany i dokładny nowy tekst.</li><li>Po uzgodnieniu zmian oddaj ustalone eksporty i pliki źródłowe do wskazanego folderu.</li></ol></section><footer class="hb-footer">To zapis ustaleń z chwili pobrania, bez wspólnej edycji na żywo. Zdjęcia i pliki źródłowe są przekazywane osobno lub dostępne w podanych folderach. Ten dokument nie potwierdza akceptacji projektu do publikacji.</footer></article>`;
}
export const handoffDocument=body;
export const handoffStyles=`
.handoff-paper{font-family:-apple-system,BlinkMacSystemFont,'Segoe UI',Arial,sans-serif;color:#344052;background:#fff;max-width:1080px;padding:44px 52px;margin:0 auto;line-height:1.65;font-size:16px;box-sizing:border-box}.handoff-paper *{box-sizing:border-box}.hb-header{display:flex;justify-content:space-between;align-items:center;gap:20px;border-bottom:1px solid #e6e9ee;padding-bottom:25px}.hb-brand svg{display:block;width:196px;height:auto}.hb-brand{font-size:20px;font-weight:750;letter-spacing:-.6px;color:#293341;line-height:1.1}.hb-brand b{color:#e75427}.hb-brand span{display:block;font-size:11px;font-weight:600;letter-spacing:.16em;color:#596677;margin-top:9px}.hb-date{font-size:12px;color:#596677}.hb-title{margin:32px 0}.hb-title>span{color:#e0764d;font-size:12px;letter-spacing:.15em;font-weight:650}.handoff-paper .hb-title h1{font-size:32px;line-height:1.18;letter-spacing:-.04em;margin:10px 0 12px;overflow-wrap:anywhere;color:#303c4e}.hb-title p{font-size:14px;color:#596677;margin:0}.hb-attention{background:#fff9f2;border:1px solid #f1dec9;border-radius:9px;padding:16px 20px;font-size:14px;color:#795030;break-inside:avoid}.hb-attention strong{font-weight:650}.hb-attention ul{padding-left:18px;margin:8px 0 0;line-height:1.8}.hb-section{margin-top:30px}.handoff-paper .hb-section h2{font-size:16px;color:#5c6a7e;margin:0 0 18px;letter-spacing:-.02em;border-bottom:1px solid #e8ecf1;padding-bottom:12px}.hb-grid{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:18px}.handoff-paper dl{margin:0}.hb-entry{margin:0 0 18px;min-width:0;break-inside:avoid}.hb-entry dt{font-size:12px;color:#596677;margin:0 0 5px;font-weight:600}.hb-entry dd{margin:0;font-size:14px;color:#435267;white-space:pre-wrap;overflow-wrap:anywhere;line-height:1.75}.handoff-paper a{color:#b76a43;text-decoration:underline;text-underline-offset:3px;overflow-wrap:anywhere}.hb-missing{font-size:14px;color:#815331;background:#fff8eb;padding:3px 6px;border-radius:4px}.hb-material{border:1px solid #e2e7ee;border-radius:12px;margin-top:18px;padding:25px 27px}.hb-material-heading{display:flex;align-items:center;gap:14px;margin-bottom:24px}.hb-material-heading>span{display:grid;place-items:center;width:35px;height:35px;background:#fff1e9;color:#d77c54;border-radius:9px;font-size:14px;flex-shrink:0}.handoff-paper .hb-material-heading h3{font-size:19px;line-height:1.3;margin:0;color:#44526a;letter-spacing:-.025em;overflow-wrap:anywhere}.hb-material-heading p{font-size:12px;color:#596677;margin:5px 0 0}.hb-context{border-top:1px solid #e9edf2;margin-top:14px;padding-top:15px}.hb-context h4{font-size:14px;font-weight:600;color:#596677;margin:0 0 12px}.hb-context .hb-entry dd{font-size:14px;color:#596677}.hb-delivery ol{font-size:14px;color:#596677;line-height:1.85;margin:0;padding-left:20px}.hb-delivery li{padding-left:5px;margin-bottom:6px}.hb-footer{border-top:1px solid #e8ecf2;padding-top:18px;margin-top:30px;font-size:12px;color:#596677;line-height:1.8}@media(max-width:650px){.handoff-paper{padding:25px 20px}.hb-header{align-items:flex-start;gap:15px}.hb-brand{font-size:18px}.hb-date{max-width:115px;font-size:12px;text-align:right}.handoff-paper .hb-title h1{font-size:25px}.hb-grid{grid-template-columns:1fr;gap:0}.hb-material{padding:20px 16px}.handoff-paper .hb-material-heading h3{font-size:18px}.hb-attention{padding:15px}.hb-entry dd{font-size:14px}}@media print{@page{size:A4;margin:14mm}.handoff-paper{max-width:none;padding:0;font-size:11px}.hb-grid{grid-template-columns:repeat(3,minmax(0,1fr));gap:15px}.hb-header{break-after:avoid}.hb-title{margin:20px 0}.handoff-paper .hb-title h1{font-size:25px}.hb-attention{break-inside:auto}.hb-section h2,.hb-material-heading{break-after:avoid}.hb-material{padding:18px 20px;break-inside:auto}.hb-entry{break-inside:avoid}.hb-entry dd{font-size:10px;color:#475267}.hb-context .hb-entry dd{font-size:10px;color:#6c7789}.hb-footer{font-size:9px}.handoff-paper a{color:#6c7382}*{-webkit-print-color-adjust:exact;print-color-adjust:exact}}
`;
export function handoffHTML(campaign){return `<!doctype html><html lang="pl"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><meta http-equiv="Content-Security-Policy" content="default-src 'none'; style-src 'unsafe-inline'; base-uri 'none'; form-action 'none'"><title>${esc(campaign.brief.name||'Kampania')} — brief dla grafika</title><style>body{margin:0;background:#f3f5f8} @media print{body{background:white}}${handoffStyles}</style></head><body>${body(campaign)}</body></html>`;}
const input=(key,field,value,extra='')=>`<label>${field.label}${field.area?`<textarea ${extra} maxlength="${field.max}" rows="3" placeholder="${esc(field.hint)}">${esc(value)}</textarea>`:`<input ${extra} type="${field.type||'text'}" ${key==='format'?'list="creativeFormats"':''} maxlength="${field.max}" placeholder="${esc(field.hint)}" value="${esc(value)}">`}</label>`;
export function handoffView(campaign,{mode='edit',focusId}={}){
 const common=cleanHandoff(campaign.handoff),nodes=campaign.flow.nodes.filter(n=>n.type==='creative'),selected=selectedCreatives(campaign),gaps=handoffGaps(campaign);
 const heading=`<section class="handoff-heading"><div><span class="eyebrow">PRESCOT LED / ZLECENIE KREACJI</span><h1>Brief dla grafika</h1><p>${esc(campaign.brief.name)} · <span data-handoff-count>${selected.length}</span> materiałów do przekazania</p></div><a class="button secondary" href="#guide/${campaign.goal}/designer">${icon('note')} Jak przekazać zadanie?</a></section><div class="handoff-toolbar"><nav aria-label="Widok briefu dla grafika"><button data-action="handoff-mode" data-mode="edit" aria-pressed="${mode==='edit'}" class="${mode==='edit'?'active':''}">${icon('plan')} Uzupełnij brief</button><button data-action="handoff-mode" data-mode="preview" aria-pressed="${mode==='preview'}" class="${mode==='preview'?'active':''}">${icon('layout')} Podgląd dla grafika</button></nav><div><button class="button secondary" data-action="print-handoff" ${selected.length?'':'disabled'}>${icon('print')} Drukuj / PDF</button><button class="button primary" data-action="download-handoff" ${selected.length?'':'disabled'}>${icon('download')} Pobierz HTML</button></div></div>`;
 if(mode==='preview')return heading+`<p class="handoff-snapshot-note">Tak wygląda pobrany plik. Zawiera wybrane kreacje i ustalenia tej kampanii. Zdjęcia i pliki projektowe przekaż w podanych folderach.</p><div class="handoff-preview"><style>${handoffStyles}</style>${body(campaign)}</div>`;
 return heading+`<section class="handoff-intro"><span class="stage-icon orange">${icon('play')}</span><div><h2>Co ma przygotować grafik?</h2><p>Wybierz kreacje z mapy i dopisz konkretne wymagania. Na końcu sprawdź podgląd. Pobrany HTML grafik otworzy u siebie w przeglądarce, bez instalacji aplikacji.</p></div></section><section class="handoff-common"><div class="section-heading"><div><span class="eyebrow">WSPÓLNE DLA WYBRANYCH KREACJI</span><h2>Osoby, materiały i termin</h2></div><span class="save-label" data-save-label></span></div><div class="handoff-fields">${Object.entries(handoffFields).map(([key,field])=>input(key,field,common[key],`data-handoff-field="${key}"`)).join('')}</div></section><section class="handoff-deliverables"><div class="section-heading"><div><span class="eyebrow">ETAPY „KREACJA” Z TWOJEJ MAPY</span><h2>Materiały do wykonania</h2></div><a class="text-link" href="#map/${campaign.goal}">${icon('plus')} Dodaj etap na mapie</a></div><datalist id="creativeFormats">${formatExamples.map(f=>`<option value="${esc(f)}"></option>`).join('')}</datalist>${nodes.length?nodes.map((node,i)=>{
  const d=cleanDesign(node.design),context=creativeContext(campaign,node.id),open=focusId?node.id===focusId:i===0;
  return `<details class="deliverable-card" data-deliverable="${node.id}" ${open?'open':''}><summary><span class="stage-icon pink">${icon('play')}</span><span><strong>${esc(node.title)}</strong><small>${esc(context.channels.map(n=>n.title).join(' / ')||'Kanał do ustalenia')}</small></span><span class="deliverable-included" data-included-label="${node.id}">${d.include?'Do przekazania':'Pominięta w pliku'}</span>${icon('chevronDown')}</summary><div class="deliverable-content"><div class="deliverable-include"><label><input type="checkbox" data-design-node="${node.id}" data-design-field="include" ${d.include?'checked':''}>Przekaż tę kreację grafikowi</label><button class="text-link" data-action="open-creative" data-id="${node.id}">Ustalenia na mapie ${icon('arrow')}</button></div><div class="deliverable-concept"><span>POMYSŁ ZAPISANY NA MAPIE</span><p>${esc(node.note||'Dodaj pomysł w ustaleniach etapu.')}</p></div><div class="design-fields">${['format','destination','headline','cta','body','visual','assets'].map(key=>input(key,designFields[key],d[key],`data-design-node="${node.id}" data-design-field="${key}"`)).join('')}</div><p class="format-note">Rozmiary z listy są przykładami roboczymi. Dopasuj format do ustalonego miejsca emisji; dla wideo wpisz też długość i sposób przygotowania napisów.</p></div></details>`;
 }).join(''):`<div class="handoff-empty"><h3>Dodaj pierwszy materiał.</h3><p>Na mapie wybierz „Dodaj etap” → „Kreacja”. Nowy materiał pojawi się tutaj.</p><a class="button primary" href="#map/${campaign.goal}">Otwórz mapę ${icon('arrow')}</a></div>`}</section><details class="handoff-check"><summary>${icon('note')}<span data-handoff-gap-count>${gaps.length?`${gaps.length} ustaleń do uzupełnienia`:'Podstawowe informacje są uzupełnione'}</span>${icon('chevronDown')}</summary><ul data-handoff-gaps>${gaps.map(g=>`<li>${esc(g)}</li>`).join('')}</ul><p>Brakujące pola będą oznaczone również w pobranym briefie. Przejrzyj treści z zespołem przed przekazaniem zadania.</p></details><div class="handoff-share"><div>${icon('download')}<span><h3>Przekaż grafikowi plik i dostęp do materiałów.</h3><p>HTML lub PDF jest podglądem ustaleń z chwili pobrania. Po zmianach pobierz nową wersję.</p></span></div><button class="button primary" data-action="handoff-mode" data-mode="preview">Sprawdź podgląd ${icon('arrow')}</button></div>`;
}
export function refreshHandoff(campaign,root=document){
 const selected=selectedCreatives(campaign),gaps=handoffGaps(campaign);
 root.querySelectorAll('[data-handoff-count]').forEach(el=>el.textContent=selected.length);
 root.querySelectorAll('[data-action="download-handoff"],[data-action="print-handoff"]').forEach(el=>el.disabled=!selected.length);
 const counter=root.querySelector('[data-handoff-gap-count]');if(counter)counter.textContent=gaps.length?`${gaps.length} ustaleń do uzupełnienia`:'Podstawowe informacje są uzupełnione';
 const list=root.querySelector('[data-handoff-gaps]');if(list)list.innerHTML=gaps.map(g=>`<li>${esc(g)}</li>`).join('');
 root.querySelectorAll('[data-included-label]').forEach(el=>{const n=campaign.flow.nodes.find(n=>n.id===el.dataset.includedLabel);el.textContent=n?.design?.include===false?'Pominięta w pliku':'Do przekazania';});
}
